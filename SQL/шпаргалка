-- считает приблизительное количество уникальных эелементов
APPROX_COUNT_DISTINCT

-- считает среднее для группы (group by) или для каждого окна (over)
AVG

-- считает check-сумму, помогает определить изменения в данных
CHECKSUM_AGG 

-- количество элементо в в группе
COUNT

-- возвращает bifint вместо int как в COUNT
COUNT_BIG

-- проверить, является ли данная строчка каким-то итогом от rollup, cube, grouping set
GROUPING

-- показывает первый или последний элемент в этой группе
FIRST_VALUE/LAST_VALUE

-- используется для получения определенного персентиля в группе (среднее / опредленное значение)
PERCENTILE_CONT / PERCENTILE_DISC

-- разделение множества на группы по elem_in_group элементов
NTILE(elem_in_group: int)

-- переворачивает вертикальные данные в горизонатльыне для отчетов, и наоборот 
PIVOT / UNPIVOT

-- для поведения итогов, используется вместе в group by
WITH ROLLUP 